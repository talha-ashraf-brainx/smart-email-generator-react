# OpenAI API key — server-side only, never exposed to the client bundle
OPENAI_API_KEY=sk-your-key-here

# Model used for generation (optional, defaults to gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Port for the production server (npm start). The Vite dev server uses its own port (5173).
PORT=3000
